name: Keep Render Backend Alive

on:
  schedule:
    - cron: "*/5 * * * *"  # Every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Backend Health Route
        run: |
          curl -s https://perfumery-2lhj.onrender.com/health
